package { 'ack':
  ensure   => present,
  provider => brewcask,
}

package { 'rar':
  ensure   => present,
  provider => brewcask,
}

package { 'nano':
  ensure   => present,
  provider => brewcask,
}
